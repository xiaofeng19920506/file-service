---
description: VIP 视频页访问权限（vip 与 admin）
globs: shared/src/permissions.ts,frontend/src/lib/permissions.ts,frontend/src/App.tsx,frontend/src/components/PageNavTabs.tsx,backend/api/src/youtube-*.ts
alwaysApply: false
---

# VIP 视频访问权限

## 谁可以访问 VIP 页（`#/vip`）

- **角色**：`vip`、`admin`（`canAccessVipVideo`）
- **路由**：`#/vip` → `VipVideoPage`
- **API**：`resolvePathAccessLevel` 为 `vip_video` 的路径（视频缓存/流、VIP 搜索与热门等）

## 播放列表：视频 vs 仅音频

- **`canPlayPlaylistVideo`**：`worship_team` / `creator` / `admin` / `vip` 可切换 YouTube **视频**模式；**`member` 仅 MP3 音频**，无视频切换、不挂载 `YoutubePlaylistPlayer`。
- 权限定义：`shared/src/permissions.ts`；前端 `permissionsForRole` 暴露 `canPlayPlaylistVideo`。

`isVipOnlyRole` 仅 `vip`（登录后强制留在 VIP 页）。

## 实现约定

- 权限单一来源：`shared/src/permissions.ts`；前端 `frontend/src/lib/permissions.ts` 保持相同逻辑。
- 导航：`PageNavTabs` 在 `canAccessVipVideo` 为真时展示「VIP」入口。
- 播放列表：`readPlaylistPlaybackMode(canPlayPlaylistVideo)`，成员恒为 `audio`。
- 扩展访问角色时同步改 shared、前端 permissions、测试与 UI 门禁。

## VIP 视频缓存编码

- **主格式**：`YOUTUBE_VIDEO_YTDLP_FORMAT`（`shared/src/youtube-video-extract.ts`）仅选 **H.264 (avc) + AAC**，禁止 VP9/HEVC 回退，兼容 iOS / Android / 鸿蒙 WebView。
- **转码回退**：无原生 H.264 时用 `YOUTUBE_VIDEO_YTDLP_FORMAT_TRANSCODE` + ffmpeg `libx264`（较慢，输出仍为 H.264 MP4）。
- 黑屏仅有声时前端可 `force` 重缓存（`resetYoutubeVideoForReextract`）。
