---
description: 优先最快完成任务，需要时并行开多个 agent
alwaysApply: true
---

# 速度优先

- 用户要**最快速度**完成任务，不要为了“更稳妥”而串行慢慢做。
- 能并行的立刻并行：读文件、改无关文件、搜索、验证。
- 任务可拆（查因 / 改前端 / 改后端 / 同步 NAS）时，**直接开多个 agent**，不要一个人慢慢啃。
- 已经能判断根因时，自己改，不要再开 agent 重复调查。
- 不要为了确认而反复问用户；按仓库既有约定直接做完（含 commit/push、覆盖 `file-service-nas`，勿动 `.env`/`data/`）。
